# Infovis-assignment - Visualization of Child Mortality related data

## How to clone - simply download the zip from the repository, or clone the repo via CLI
### Upon download, works out of the box, however will need to run the project.
### Either run through an IDE (e.g. on Webstorm - Run index.html) or in the terminal with ```npx serve .``` 
### Dependencies: d3.js